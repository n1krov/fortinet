---
Tema: "[[CLI]]"
---
`config system alias`

`edit nombre`

`set command "comando"`

ejemplo de uso desde la CLi
 `FORTI-01 (nombre) # set command "show interface port1"`

`end` guardar cambios y vuelve a raiz
`next` guarda cambios y se queda en elmismo contexto (nombre)
`abort` cancela